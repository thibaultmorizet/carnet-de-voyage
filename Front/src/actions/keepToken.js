export const KEEP_TOKEN = 'KEEP_TOKEN';

export const keepToken = (value) => ({
  type: KEEP_TOKEN,
  value,
});

export const LOG_IN = 'KEEP_TOKEN';

export const login = () => ({
  type: LOG_IN,
});
