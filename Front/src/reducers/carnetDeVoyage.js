export const initialState = {
  loading: true,
};

const createStep = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default createStep;
